# Optional client URL when Vite and the server run on different origins.
VITE_TOPTHIS_SERVER_URL=http://127.0.0.1:3000

# Optional server settings (safe local defaults are used when omitted).
PORT=3000
HOST=127.0.0.1
# TOPTHIS_DATABASE_PATH=./data/topthis.sqlite
